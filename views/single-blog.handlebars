<article class="post">
  <div class="title">
    <h1>{{post.title}}</h1>
        <p>{{post.post_text}}</p>
  </div>
  <div class="meta">
    by {{post.user.username}} on
    {{post.created_at}}
    |
    <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
  </div>
</article>